# Decision log

Newest at the bottom. One line of "why" each — enough to remember, not an essay.

| Date | Decision | Why |
|---|---|---|
| 2026-07-15 | macOS only, native Swift/SwiftUI | Native provides the smallest, most trustworthy macOS utility |
| 2026-07-15 | All AI local via MLX (Qwen ~3B 4-bit + small embedding model) | Privacy IS the product; the selected model fits the supported hardware profile |
| 2026-07-15 | Small LLM + embeddings (not embeddings-only) | Founder chose quality of suggested filenames over minimal footprint |
| 2026-07-15 | SwiftPM executable, no .xcodeproj | Builds with CLT Swift 5.8 today; simpler for a beginner to navigate; Xcode needed only from M3 |
| 2026-07-15 | Watch `~/Downloads` only in MVP | Highest-churn folder; smallest permission ask |
| 2026-07-15 | Files move ONLY on explicit accept; all mutations via [[History-Undo]] | Trust is everything for an app that touches user files |
| 2026-07-15 | Repo doubles as the founder's Obsidian vault | One source of truth; `[[wiki-links]]` in docs mirror code dependencies |
| 2026-07-15 | Six-agent Claude Code team (`.claude/agents/`), founder talks only to the main session | Keeps any one agent from being overloaded; founder stays in product language |
| 2026-07-15 | Payments later via merchant-of-record (Lemon Squeezy/Paddle) | They handle checkout, global sales tax, and license keys — no payment code to write |
| 2026-07-16 | Added `swift-build-resolver` and `swift-reviewer` agents, cherry-picked from affaan-m/ECC (MIT-licensed, github.com/affaan-m/ECC) and trimmed to this project | ECC's full framework (67 agents, cross-tool installer touching global configs) was too broad for one project; verified the two files were safe (no network calls, no destructive commands) and rewrote them into our house style before adding — project-scoped only, nothing global |
| 2026-07-16 | Expanded ECC adoption: added `planner`, `silent-failure-hunter`, `code-simplifier` agents + engineering-rules + learnings system (all adapted from affaan-m/ECC, MIT) | Founder asked to adopt more of ECC after studying it; cherry-picked & hand-vetted files only — ECC's installer, hooks, MCP servers, and `performance-optimizer` (web-specific) deliberately skipped; a Swift-specific perf agent will be written at M3 when it matters |
| 2026-07-16 | **No Mac App Store — ever.** Distribute as a direct download from our website (like Claude Desktop / Wispr Flow) | Avoids Apple's 30% cut, review delays, and sandbox rules that would cripple a file-moving app. Still requires the $99/yr Apple Developer account at Phase 3 for notarization (the stamp that makes downloads open without scary warnings) — founder approves before any spend |
| 2026-07-17 | PDFs are rejected only when `isLocked`, not when `isEncrypted` | Bank statements and e-tickets are often owner-password "encrypted" yet open fine — rejecting them would fail on exactly the files this app is for |
| 2026-07-17 | Images decode through a capped 4000px thumbnail (`CGImageSourceCreateThumbnailAtIndex`), never full-size | A tiny PNG can decompress to gigabytes (decompression bomb); bounding pixels, not file bytes, is what actually caps memory |
| 2026-07-17 | Dev-only env flag `FILE_ORGANIZER_DEBUG_SNIPPETS=1` may print sanitized 200-char snippet previews | Founder-approved privacy exception for debugging; off by default, normal runs log filenames/methods/counts only, and it's on the M6 release checklist to remove |
| 2026-07-17 | 60-second per-file extraction timeout in `PipelineModel`; late results dropped | A hung read of a huge/malformed file must show "Took too long to read", not sit on "Reading…" forever |
| 2026-07-17 | Full Xcode installed; `./build.sh` now builds via SwiftPM (`swift build -c release`) | Required for M3's MLX dependency; the swiftc fallback stays in the script for reference |
| 2026-07-17 | Second ECC sweep (founder-requested): adopted `ai-reviewer` (from mle-reviewer, rewritten for on-device) + `tdd-guide` + [[ai-engineering]] guide; deferred `database-reviewer` to M4 and `a11y-architect`/design skills to M5 (just-in-time); skipped `prompt-optimizer` (optimizes user prompts, not app prompts), `cost-aware-llm-pipeline` (API billing — our AI is free local), and the marketing pack (founder deselected) | Match tooling to the milestone that needs it; every adoption hand-vetted, project-local |
| 2026-07-17 | M3 planning must weigh MLX+Qwen (locked earlier) against Apple's built-in FoundationModels on-device model (macOS 26) | FoundationModels would eliminate the 2 GB download entirely; quality/control trade-offs — founder decides at M3 kickoff with the planner's comparison |
| 2026-07-17 | **Founder chose Apple FoundationModels over MLX+Qwen** for all M3 AI (summaries + names; embeddings via built-in NLEmbedding) | Zero downloads, zero third-party AI code, nothing to keep cost-free because it already is; `AIProviding` protocol keeps the MLX swap-back path open if quality disappoints |
| 2026-07-18 | Fresh `LanguageModelSession` per generation — never reused across files | Sessions are stateful chat transcripts: reuse would leak one file's content into the next file's prompt (privacy + injection), overflow the context window, and error when a timed-out call still occupies the session |
| 2026-07-18 | Sampling: greedy first, one retry on decoding failure only, with default sampling; prompt template + `@Generable` guides live as code constants in `AIEngine`/`PromptBuilder` | Greedy = same file, same suggestion (predictable for the founder's demo); a greedy retry would reproduce the identical malformed answer, so the retry must change sampling — and retrying a shape-correct-but-unusable name at all just burns 60 s |
| 2026-07-18 | Every model string passes an output sanitizer before UI or filesystem: `FilenameSanitizer` (separators, control/bidi/invisible Unicode, lookalike-separator folding, code-applied extension, 100-char cap) and `SummarySanitizer` (one line, 120 chars) | Model output is untrusted input — output-side enforcement is the injection-defense layer that doesn't depend on the model behaving |
| 2026-07-18 | Adopted `database-reviewer` agent (from ECC, MIT — the M4 just-in-time adoption approved 2026-07-17), rewritten from cloud-Postgres/Supabase scope to local-SQLite scope | Original reviewed for RLS/pooling/pg_stat — none of it applies to one local DB file; ours reviews bound parameters, checked return codes, single-writer discipline, index privacy, corruption recovery |
| 2026-07-18 | Folder index: raw SQLite via `import SQLite3` (built into macOS), single connection owned by one actor, WAL, busy-timeout, every value bound — no third-party DB package | Keeps the zero-dependency posture; a Swift DB wrapper would be the repo's first supply-chain surface for a ~200-line need |
| 2026-07-18 | **Founder:** folder profiles read file names AND briefly extract text from up to 3 recent files per folder (all on-device) | Founder chose accuracy over names-only scanning; privacy copy in Settings was rewritten to say so honestly ("…briefly looks inside a few files. Nothing leaves your Mac.") |
| 2026-07-18 | **Founder:** when Apple Intelligence is off, show NO folder suggestion at all (no name-only guessing) | Founder prefers silence over lower-confidence suggestions; matching runs only after a successful AI suggestion |
| 2026-07-18 | Matching: cosine over pre-normalized vectors (normalized once at profile build; non-unit/mismatched vectors excluded at match time, never renormalized); threshold 0.6 as QA-tunable start; ties within 0.02 → earliest-added folder wins | Below-threshold must degrade to an honest "No folder fits" — never force a bad destination; deterministic ties keep the demo predictable |
| 2026-07-19 | **Match threshold 0.6 → 0.80, PROVISIONAL** — calibrated against real NLEmbedding on macOS 26.5 (Apple Intelligence now enabled) on a *synthetic* representative set with stand-in AI summaries; pinned by `FolderMatchAcceptanceTests` (live NLEmbedding + extraction, no mocks). Real-folder + real-AI QA remains the M4 sign-off gate | NLEmbedding compresses cosine into a high band: real-home files score 0.84–0.94 (always ranking the right folder #1); strays — including near-misses (a bank statement vs Tax Documents at **0.78**, a utility bill, a restaurant menu vs Recipes) — top out at 0.78. So 0.6 force-matched strays. Window (0.78, 0.84]; 0.80 leans to recall. Near-miss margin is thin (~0.02), tolerable only because a match merely *suggests* (M4 never moves) and "No folder fits" is the honest fallback. Not final: same 8+3 synthetic points chose and test 0.80 (circular), and real messy files may score lower. Re-tune via `CALIB_OUT=… swift test --filter FolderMatchAcceptanceTests` |
| 2026-07-19 | Adopted ECC's `a11y-architect` at M5 kickoff, rewritten from Web/ARIA+iOS+Android down to macOS/SwiftUI scope; report-only, wired into pipeline steps 2 & 7 | The popup is the first design-critical, assistive-tech-sensitive UI; a dedicated accessibility lens (VoiceOver, keyboard, focus, motion, contrast) belongs in the review round. Stays in the a11y lane — visual/UX critique defers to the gstack `/design-*` skills |
| 2026-07-23 | **M5 Accept seam is `SuggestionAccepting.accept(_:)` returning Void for M5** (impl `NoMoveAccepting` = zero filesystem work); `AcceptedSuggestion` carries the destination folder's **`Int64` store identity**, not just its name | The popup moves no file in M5, so it needs no failure channel yet; carrying identity (not name) lets M6 move to the right folder even when two folders share a leaf name. **M6 MUST widen the seam to report failure and only close the popup on success** — see [[Popup-UI]] |
| 2026-07-23 | **Post-Accept: the popup just closes quietly** — no "accepted" confirmation, no checkmark | Founder decision; in M5 nothing moves, so any "done" affordance would be untrue. The honest confirmation lives in M6 when a file actually moves |
| 2026-07-23 | Popup auto-dismiss (~15 s) does nothing but close; **disabled entirely** under VoiceOver / Full Keyboard Access, paused while hovering or editing | A timed disappearance must never decide a file's fate, and assistive-tech users need unbounded time to reach and act on the panel |
| 2026-07-23 | Extraction dev-logging (`logResult`, incl. the `FILE_ORGANIZER_DEBUG_SNIPPETS` content preview) wrapped in `#if DEBUG` — compiled out of release entirely | Founder-approved privacy hardening (security-auditor O1): a runtime env flag alone left the content-printing code in shipped binaries; `#if DEBUG` removes the path so no env var can surface file content in release. Supersedes the 2026-07-17 "on the M6 release checklist to remove" plan — it's done |
| 2026-07-23 | Kept the decorative pencil icon in the popup name field (a11y-hidden, non-interactive) | Matches the founder-approved mockup; harmless. Drop only on founder request |
| 2026-07-23 | **Workflow tune-up (founder-requested):** trimmed the always-loaded `CLAUDE.md` — status detail now points to [[milestones]], this provenance history moved here — and added mid-session-compaction guidance to token-hygiene | `CLAUDE.md` is re-read every turn of every session, so weight there is the one recurring token cost, and the blow-by-blow already lived in [[milestones]]. Leaner instruction file, zero information lost, the review pipeline itself unchanged |
| 2026-07-23 | **Re-audited ECC for workflow/org enhancements; imported none of its machinery — refreshed the Swift rules instead** (typed throws, parameterized tests, `--enable-code-coverage`) | The high-value ECC agents were already adopted; the rest is excluded-by-policy (hooks/global config/installer), redundant (its WORKING-CONTEXT = our [[milestones]]; feature-development = our lightweight path), or irrelevant (business/multi-language kitchen sink). ECC's one useful token idea (`strategic-compact`) ships as an edit-time Node hook — distilled its *guidance* into token-hygiene as plain text, no hook. Don't re-mine ECC for machinery without a specific need |
| 2026-07-23 | **System-consistency audit: privacy rule tightened to "zero network calls"** across `CLAUDE.md`, `security-auditor`, `swift-builder`, `ai-reviewer`, [[architecture]], [[engineering-rules]]; `ai-reviewer`/`swift-builder` briefs rewritten off the old MLX framing (incl. stale "macOS 13+" target → macOS 26+); [[architecture]] status caught up to M5 | The old "one permitted network call: the model download" exception was dead since the 2026-07-17 FoundationModels switch (nothing to download) — leaving it in place meant the security auditor would *excuse* egress it should flag as critical. The MLX fallback exception is noted as dormant, reactivatable only with founder approval |
| 2026-07-28 | **M6 founder decision 1 — fail closed:** if the history row can't be written, the file is **not** moved | An unrecorded move is an un-undoable move; the whole promise of M6 is that every change is reversible, so no-record must mean no-change |
| 2026-07-28 | **M6 founder decision 2 — destination folder *gone* at move time → rename in place in Downloads**, and say so honestly | The user asked for a better name and a home; if the home is gone, still deliver the name rather than failing entirely. Narrowed by decision 7 — covers *gone*, not *blocked* |
| 2026-07-28 | **M6 founder decision 3 — retention: 200 rows, plus "Clear history" in Settings** | Enough history to undo anything the user still remembers doing, small enough that the file stays trivial; the clear button is the privacy escape hatch |
| 2026-07-28 | ~~**M6 founder decision 4** — dropdown switches to `.menuBarExtraStyle(.window)`~~ — **SUPERSEDED by decision 6** | — |
| 2026-07-28 | **M6 founder decision 5 — during a move, Dismiss stays enabled and becomes "Hide"**; the move continues and its result lands in history. Accept is the only disabled control, and **Esc must always be answered, never inert** | Hiding is not cancelling: once a move has started, taking it back is a second filesystem mutation, not an undo of intent. A dead Esc key is the thing keyboard users hit first |
| 2026-07-28 | **M6 founder decision 6 — the dropdown keeps its `.menu` style; history and Undo move to the Settings window** | The founder was offered the panel-style dropdown from decision 4 and chose the conservative option: nothing about today's dropdown changes visually. Settings is a real window, so the accessibility block that forced decision 4 (plain `Text` rows become *disabled* `NSMenuItem`s — unreachable by keyboard and VoiceOver) simply does not apply there |
| 2026-07-28 | **M6 founder decision 7 — a destination blocked by macOS privacy (TCC) is a failure, not a fallback:** the popup says so and offers "Open System Settings"; the file is neither moved nor renamed | Distinct from decision 2 because a TCC denial is a one-time fixable permission problem — renaming in Downloads would hide a problem the user can actually solve |
| 2026-07-28 | **Move history lives in its own `history.db`**, separate from the folder index's `index.db` | Different lifetimes and different blast radius: the index is derived data that can be rebuilt from disk at any time, the history is the *only* record of what the app did to the user's files and can never be regenerated. One corrupt file must not take both down |
| 2026-07-28 | `history.db`: WAL journal, **`synchronous=FULL`** (the index uses NORMAL), `secure_delete=ON`, busy-timeout, single connection owned by one actor, every value bound | FULL costs an fsync per commit and buys durability across a power loss — correct when the row is what makes a move undoable, wasteful for a rebuildable index. `secure_delete` is what makes "Clear history" mean the filenames are actually gone rather than merely unlinked |
| 2026-07-28 | **Corruption policy differs from the index's:** a corrupt `history.db` is moved aside intact and the user is told; a corrupt `index.db` is silently rebuilt | A rebuilt index is free and identical; a discarded history silently destroys the user's ability to undo real moves, so it must be preserved and surfaced, never quietly reset |
| 2026-07-29 | **Founder kept both contested M6 UI calls as built:** the failure button reads **"Try again"** (not "Accept"), and a move that succeeded *with a change* holds the popup open with an "OK" button | Founder call on 2026-07-29 after both were flagged as diverging from the approved mockup. The behaviour stands; the missing guard against a second Accept in that state is a defect to fix regardless — see finding F13 |
| 2026-07-30 | **Docs reorganised into `product/` (what we're building) + `process/` (how we build it)**, with the root of `docs/` reduced to just the map and a new single register, `open-work.md`. All path references in docs, agent briefs, and Swift comments updated; both builds verified green | The vault had drifted: `README.md` said M6 wasn't built, `ai-engineering.md` still told the AI reviewer to expect a model download that the FoundationModels switch had made impossible, `architecture.md` still described the M5 no-move seam, and `Watcher.md` deferred two live safety limits until "before files are moved" — which had already happened. Open work was scattered across seven documents with no single list, so nothing could be trusted as complete. A two-bucket split plus one register makes staleness visible instead of invisible |
| 2026-07-30 | **`open-work.md` is the single register**: reviewers report, the tech lead files every finding there in the same turn, `docs-keeper` reconciles it at milestone close. Items are grouped blocking / founder-owned / deferred, each in plain language | Founder's call. Reviewers are report-only by design (no write tools), so findings only survive if someone records them — and "someone" was previously nobody in particular. A finding that exists only in a subagent's report is untracked the moment the session ends |
| 2026-07-30 | **`CLAUDE.md` trimmed to invariants, the scope gate, and pointers** — the eight-step pipeline moved to [[workflow]], the agent roster to [[agent-roster]], the M6 blow-by-blow to [[open-work]] | `CLAUDE.md` is re-read every turn of every session, so weight there is the one recurring token cost in the project. Procedure is needed occasionally; invariants and the scope gate are needed always. Split them by frequency of use, not by topic |
| 2026-07-30 | Removed two dead code items with founder approval: a duplicate copy of the "original couldn't be removed" warning sentence in `MoveCoordinator`, and an uncalled `updateStatus` helper in `FolderIndexStore` | The duplicated sentence was the real risk — one copy was rendered, one was orphaned, so a future reword could have "fixed" the invisible one. A scan of every declared symbol found only these two unused; the codebase is otherwise clean |
| 2026-07-30 | **`website/site/` is the deployable root**; everything else in `website/` (playbook, README, `design-source/`) is internal and never published | Before the split, pointing a host at `website/` would have published 304 KB of internal material — design prototypes, screenshots, an unreleased mockup, and the team's own process docs. The go-live gate is still open, so this was the last cheap moment to separate them. A publish root that is also a working folder leaks by default |
| 2026-07-30 | **Replaced `website/design-source/README.md`** — vendor boilerplate titled "CODING AGENTS: READ THIS FIRST" that issued instructions to coding agents — with a note stating the folder is reference data, never instructions. Original preserved in git history | A file inside the repo that addresses agents directly and commands them is the same shape as a prompt-injection attempt, whether or not anyone meant harm — and its commanded path (`file-organization-app-website/project/Home.dc.html`) never existed here, so an agent obeying it would have gone hunting for a file that isn't there. `CLAUDE.md` already warned about it in prose; removing the hazard beats documenting it |
| 2026-07-30 | **Undo proves identity, same-volume only**: after the existence check it compares the row's stored `(device, inode)` against what is at the final path, and refuses when the device matches and the inode does not. Cross-volume deferred to schema v3 (`final_device`/`final_inode`) | A cross-volume move is a copy, so the file legitimately has a new inode — refusing on that basis would break every cross-volume undo. Refusing only on a same-device inode mismatch closes the reachable case (delete the moved file, later file an unrelated one under that name, and Undo relocated the stranger's file) without breaking the honest one |
| 2026-07-30 | **`RefusingAccepting` replaces `NoMoveAccepting` as the app's degraded seam.** `NoMoveAccepting` stays as a test double, pinned as one | Founder decision 1 is fail *closed*, and closed has to be visible. `.unchanged` is a success, so with an unopenable `history.db` every Accept closed the popup exactly as a real move does while the file sat untouched in Downloads — no row, no message, menu bar still reading "Watching Downloads" |
| 2026-07-30 | **The DELETE is the commit point of "Clear history."** Nothing after it may throw; a failed `VACUUM` or checkpoint returns a distinct "cleared, but not tidied" outcome instead | The rows are gone the moment the delete commits. Reporting the tidy-up's failure as "the history couldn't be cleared" told the user the opposite of the truth and left them looking at rows that no longer existed, every Undo on which failed |
| 2026-07-30 | **A history problem is either about something that *happened* or about the table's current state**, and only the second kind is cleared by a later successful read | `refreshRecent()` runs at the end of every path that has just written a problem. Clearing unconditionally erased "that move happened, but the history couldn't be updated" microseconds after writing it — so the single most important sentence in the milestone could never reach a screen |
| 2026-07-30 | **A move's history row stores `MoveSummary.historyNote`, not the popup's full `notice`**; and `MoveError` gained `message(namingFolder:)` + a plain-language `userDetail` in place of the raw typed payload | The row derives the fallback clause from its own column, so storing the full notice printed that sentence twice. And a history row is read weeks later: "(errno 28: No space left on device)" is not something to put in front of a person, while "an incomplete copy was left in that folder" is exactly what they need |
| 2026-07-30 | **`PopupActivity.failed` carries `offersPrivacySettings`**, so the popup can show "Open System Settings" for a TCC block (founder decision 7) | The state carried only a `String`, so the view could not tell the one fixable failure from the rest without matching on prose that will be reworded. A flag is the thing that survives a copy edit |
| 2026-07-30 | **The popup panel answers Esc at the window level** (`cancelOperation`), not only via a `.cancelAction` button | Founder decision 5 says Esc must always be answered. The completed state's only button is the default-action "OK", so Esc reached nothing there. Handling it on the window makes the guarantee structural — it holds in states added later, too |
| 2026-07-30 | **`MoveHistoryStore.failNextWrite()` is a DEBUG-only, one-shot test seam** (compiled out of release; verified absent from the binary) | The branches where the file has ALREADY moved and only the row is left to write are the ones that must never lie about the user's disk (F7/F8) — and they cannot be reached from outside, because closing the store makes the *first* call fail instead, before anything moves. Same precedent as `close()`, which the store already documents as how tests simulate an unavailable history |
| 2026-07-30 | **A green suite is only evidence where the guard has been mutation-checked.** Fourteen were, at the M6 fix round; the list is in [[open-work]] | Five tests this milestone were caught passing — or hanging — against deliberately broken code. "The tests pass" and "the code is correct" had come apart, and the only way to put them back together is to break the code on purpose and watch the test go red |
| 2026-07-30 | **Session-boundary procedure is mandatory and runs at BOTH ends** — open by verifying [[open-work]] against `git status` / `git log` / a real test run, close by walking a fixed six-file list, and keep an **⚡ In flight** section written *before* expensive work rather than after it | Founder's call. Docs drift at session boundaries, and the worst boundary is the unplanned one — a `/clear`, a context limit, a closed laptop — where nothing in memory survives and only a file on disk does. The 2026-07-30 audit found four documents confidently describing an app that no longer existed; none was wrong when written. Verifying the register on open matters as much as writing it on close: a stale register is worse than no register, because it gets believed |
